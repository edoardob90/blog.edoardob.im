---
import Layout from '@/layouts/Layout.astro';
import PostsByTag from '@/components/PostsByTag.astro';
import { SITE } from '@/consts';

const title = `${SITE.title} | Tags`;
---

<Layout title={title} page='Tags'>
	<PostsByTag />
</Layout>
